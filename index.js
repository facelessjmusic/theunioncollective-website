/*
  Stub entry for this repository.

  The site's build script (node/build.js) generates `index.html` only
  and does not produce a JavaScript bundle. `package.json` points
  `main` at `index.js` for compatibility with some tools — this file
  is a lightweight placeholder.

  If you want a bundled `index.js`, modify `node/build.js` to write
  a JS file, or add a bundler step (esbuild/webpack/rollup) to
  produce one from your client-side scripts.
*/

export default {};
